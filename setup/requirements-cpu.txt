# CPU-only requirements (default configuration)
# Explicit CPU versions to ensure no GPU dependencies

# PyTorch CPU-only versions
torch>=2.4.0+cpu
torchvision>=0.19.0+cpu
torchaudio>=2.4.0+cpu

# Ensure no CUDA dependencies are pulled in
# This file can be used to explicitly override GPU installations