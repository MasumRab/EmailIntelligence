# CPU-only requirements
# These are installed in addition to base requirements.txt
# Explicitly CPU-only versions to avoid CUDA dependencies

# CPU-only PyTorch versions
torch>=2.4.0+cpu
torchvision>=0.19.0+cpu
torchaudio>=2.4.0+cpu

# CPU-optimized packages
transformers>=4.40.0
accelerate>=0.30.0

# Additional CPU-specific optimizations can be added here