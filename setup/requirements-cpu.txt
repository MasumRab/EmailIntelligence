# CPU-only requirements
# These are installed in addition to setup/requirements.txt
# CPU-optimized packages that complement the base requirements
# PyTorch CPU versions are handled via pyproject.toml CPU index

# CPU-optimized ML packages (matching base requirements versions)
transformers>=4.52.4
accelerate>=1.7.0

# CPU-specific optimizations
cpufeature>=0.2.0  # CPU feature detection for optimization

# Additional CPU-specific packages can be added here
# These will be installed alongside the base requirements