# Linux-specific requirements and overrides
# These packages are typically installed via system packages (apt)
# but listed here as pip fallbacks when system packages are not available

# Scientific computing (often installed via apt on Linux)
# Uncomment these if system packages are not available:
# numpy>=1.24.0
# scipy>=1.11.0
# matplotlib>=3.7.0
# pandas>=2.0.0
# seaborn>=0.13.0
# plotly>=5.0.0

# Linux-specific packages
notmuch>=0.29

# System integration packages (may be installed via apt)
# Uncomment if needed:
# sentencepiece>=0.2.0