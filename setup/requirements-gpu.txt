# GPU-specific requirements
# These are installed in addition to base requirements.txt
# WARNING: Requires CUDA-compatible GPU and drivers

# GPU-enabled PyTorch versions
torch>=2.4.0+cu121
torchvision>=0.19.0+cu121
torchaudio>=2.4.0+cu121

# GPU-accelerated packages
# Note: These will use GPU if available, CPU otherwise
transformers>=4.40.0
accelerate>=0.30.0

# Additional GPU-specific dependencies can be added here