# GPU-enabled requirements (Linux only)
# These override CPU versions when GPU is available/detected

# PyTorch with CUDA support (Linux only)
torch[cuda]>=2.4.0; sys_platform == "linux"
torchvision[cuda]>=0.19.0; sys_platform == "linux"
torchaudio[cuda]>=2.4.0; sys_platform == "linux"

# GPU-accelerated versions of other packages (if available)
# Add GPU-specific overrides here when available