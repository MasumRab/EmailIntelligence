# Windows-specific requirements
# These are installed in addition to base requirements.txt

# Windows-specific packages
# Note: Some packages may have different versions or installation methods on Windows

# Email processing (Windows-compatible versions)
# notmuch is not available on Windows, alternatives may be needed

# Additional Windows-specific dependencies can be added here