# Windows-specific requirements and overrides
# Windows typically requires pip installation for most packages

# PyTorch CPU-only for Windows (no CUDA support on Windows in this setup)
torch>=2.4.0+cpu
torchvision>=0.19.0+cpu
torchaudio>=2.4.0+cpu

# Windows may need specific versions of some packages
# Add Windows-specific overrides here if needed

# Windows path handling and system integration
# pywin32>=306; sys_platform == "win32"  # Uncomment if needed