# Core test dependencies
black>=25.1.0
isort>=6.0.1
flake8>=7.2.0
mypy>=1.16.0
pylint>=3.3.7
pre-commit>=3.0.0

# Test runner
pytest>=8.4.0
pytest-asyncio>=0.23.0

# Backend dependencies for tests
uvicorn[standard]>=0.30.0

# Install project in editable mode with full dependencies to ensure verify-dependencies.py passes
-e .[full]
