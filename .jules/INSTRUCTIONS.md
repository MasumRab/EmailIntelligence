1. Make sure no build artefacts (e.g. `node_modules/`, `dist/`, `*.pyc`) are staged for commit.
2. Make sure no secrets/hardcoded tokens are in the diff.
3. Make sure you have not modified read-only files.
