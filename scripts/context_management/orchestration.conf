# orchestration.conf - A shared configuration file for context management scripts.

# A space-separated list of files and directories that are managed by the orchestration framework.
ORCHESTRATED_FILES=(
    "setup/"
    "deployment/"
    "pyproject.toml"
    "requirements.txt"
    "launch.py"
)
