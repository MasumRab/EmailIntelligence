name: CI

on:
  push:
    branches: [ main, scientific ]
  pull_request:
    branches: [ main, scientific ]

jobs:
  test:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          
      - name: Install dependencies
        run: |
          uv sync --extra dev
          
      - name: Run tests
        run: |
          uv run --extra dev python -m pytest src/ modules/ tests/ -v --tb=short --cov=src --cov=modules --cov-report=xml --cov-report=term-missing --cov-fail-under=80

      - name: Run Security Scans
        run: |
          uv run --extra dev bandit -r src/ modules/
          uv run --extra dev safety check
          
      - name: Run linting
        run: |
          uv run --extra dev flake8 src/ modules/ tests/
          uv run --extra dev black --check src/ modules/ tests/
          uv run --extra dev isort --check-only src/ modules/ tests/
          
      - name: Type checking
        run: |
          uv run --extra dev mypy src/ modules/ --show-error-codes --no-strict-optional