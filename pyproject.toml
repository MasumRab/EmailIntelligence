[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "email-validator>=2.2.0",
    "httpx>=0.28.1",

    # Gradio UI
    "gradio>=4.0.0",
    "RestrictedPython>=8.0",
    "pyngrok>=0.7.0",

    # NLP and Machine Learning
    "nltk>=3.9.1",
    "textblob>=0.19.0",
    "transformers>=4.40.0",
    "torch>=2.4.0",
    "accelerate>=0.30.0",
    "sentencepiece>=0.2.0",
    "scikit-learn>=1.5.0",
    "joblib>=1.5.1",

    # Data Handling and Plotting
    "pandas>=2.0.0",
    "numpy>=1.26.0",
    "matplotlib>=3.8.0",
    "seaborn>=0.13.0",
    "scipy>=1.11.0",
    "plotly>=5.18.0",

    # System and Performance Monitoring
    "psutil>=6.0.0",

    # Google API
    "google-api-python-client>=2.172.0",
    "google-auth>=2.40.3",
    "google-auth-oauthlib>=1.2.2",

    # Security
    "bleach>=6.0.0",
>>>>>>> d094f0c (Add RestrictedPython to dependencies to fix Gradio import error)
]

[dependency-groups]
dev = [
    "accelerate>=1.7.0",
    "black>=25.1.0",
    "flake8>=7.2.0",
    "google-api-python-client>=2.172.0",
    "google-auth>=2.40.3",
    "google-auth-oauthlib>=1.2.2",
    "gradio>=5.34.0",
    "isort>=6.0.1",
    "joblib>=1.5.1",
    "mypy>=1.16.0",
    "nltk>=3.9.1",
    "pylint>=3.3.7",
    "pyngrok>=0.7.0",
    "pytest>=8.4.0",
    "python-dotenv>=1.1.0",
    "scikit-learn>=1.7.0",
    "sentencepiece>=0.2.0",
    "textblob>=0.19.0",
    "torch>=2.7.1",
    "transformers>=4.52.4",
    "uvicorn>=0.34.3",
]


[tool.black]
line-length = 100
target-version = ['py311']
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | venv
  | _build
  | buck-out
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
line_length = 100

[tool.pytest.ini_options]
# Try setting via addopts as the plugin might not be loading to register the asyncio_mode option directly
addopts = "--asyncio-mode=auto"
markers = [
    "asyncio: mark a test as asynchronous (run by pytest-asyncio)",
]
